# Project 62: CSV File Reader (Rust)

## Description
Reads a CSV file and prints its rows (requires `csv` crate).

## Learning Objectives
- Use the `csv` crate
- Parse and iterate CSV records

## How to Run
```
cargo run
```
