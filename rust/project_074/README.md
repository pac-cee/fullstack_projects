# Project 74: Simple REST API Client (Rust)

## Description
Makes a REST API call and parses JSON response (requires `reqwest` and `serde_json`).

## Learning Objectives
- Use `reqwest` for HTTP
- Parse JSON responses with `serde_json`

## How to Run
```
cargo run
```
