# Project 77: Basic File Uploader (Rust)

## Description
Uploads a file to a web server using multipart/form-data (requires `reqwest`).

## Learning Objectives
- Use `reqwest` for multipart requests
- Upload files with HTTP POST

## How to Run
```
cargo run
```
