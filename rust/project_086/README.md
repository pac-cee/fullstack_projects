# Project 86: Basic File Watcher (Rust)

## Description
Watches a directory for file changes and prints notifications (requires `notify` crate).

## Learning Objectives
- Use the `notify` crate
- React to file system events

## How to Run
```
cargo run
```
