# Project 64: Simple Config Loader (Rust)

## Description
Loads configuration from a `.toml` file using the `toml` crate.

## Learning Objectives
- Use the `toml` crate
- Deserialize config files into structs

## How to Run
```
cargo run
```
